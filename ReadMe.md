#projekat za istrazivanje podataka

git status

git pull

git add .\imefajla

git commit -m "poruka"

 git push

 git checkout -b "nova grana"

 git checkout "vec postoji grana"

 git branch

 git stash